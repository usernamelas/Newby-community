name: Build Pool Assistant Pro

on:
  push:
    branches: [ termux-builds ]
  pull_request:
    branches: [ termux-builds ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Cache Gradle packages
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Debug APK
      run: ./gradlew :android:app:assembleDebug

    - name: List APK Output Directory
      run: ls -R android/app/build/outputs/apk/

    - name: Upload Debug APK
      uses: actions/upload-artifact@v3
      with:
        name: pool-assistant-debug
        path: android/app/build/outputs/apk/debug/*.apk

    - name: Build Release APK
      run: ./gradlew :android:app:assembleRelease

    - name: List Release APK Directory
      run: ls -R android/app/build/outputs/apk/

    - name: Upload Release APK
      uses: actions/upload-artifact@v3
      with:
        name: pool-assistant-release
        path: android/app/build/outputs/apk/release/*.apk

    - name: Upload Mapping Files
      uses: actions/upload-artifact@v3
      with:
        name: mapping-files
        path: android/app/build/outputs/mapping/release/mapping.txt
